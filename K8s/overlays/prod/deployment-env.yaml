apiVersion: apps/v1
kind: Deployment
metadata:
  name: philosophizer
spec:
  template:
    spec:
      containers:
        - name: philosophizer
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod