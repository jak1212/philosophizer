apiVersion: batch/v1
kind: CronJob
metadata:
  name: philosophizer-daily-email
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: philosophizer-job
              env:
                - name: SPRING_PROFILES_ACTIVE
                  value: uat,job