apiVersion: v1
kind: Service
metadata:
  name: philosophizer-internal
  namespace: philosophizer
spec:
  type: ClusterIP
  selector:
    app: philosophizer
  ports:
    - protocol: TCP
      port: 80          # External port
      targetPort: 8080  # app's port

